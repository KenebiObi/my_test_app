import 'package:flutter/material.dart';

class SavePasswordScreen extends StatelessWidget {
  const SavePasswordScreen();

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Save Password"));
  }
}
